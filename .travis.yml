language: node_js

node_js:
  - "12.12"

jobs:
  include:
    - stage: "Lint"
      name: "Lint commit messages"
      script: commitlint-travis
    - stage: "Publish"
      name: "Run semantic release on all packages"
      script: semantic-release

notifications:
  email: false
