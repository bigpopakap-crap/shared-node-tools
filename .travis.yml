language: node_js

node_js:
  - "12.12"

jobs:
  include:
    - stage: "Lint"
      name: "commitlint-travis to verify semantic commit messages"
      script: commitlint-travis
    - stage: "Publish"
      name: "semantic-release to publish all packages"
      script: semantic-release

notifications:
  email: false
